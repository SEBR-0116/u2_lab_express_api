const db=require("../db");
const Actor=require("../models/actor")

db.on("error", console.error.bind(console, "MongoDB connection error:"));

const main= async () =>{
    const actors=[
        {
            name: 'Christian Bale',
            birthday
        }
    ]
}
